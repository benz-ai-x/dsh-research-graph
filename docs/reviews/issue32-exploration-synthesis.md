# Issue #32：图谱探索与知识综合验收

2026-09-12 至 2026-09-13，完成 #32 的 P4 → P2 → P1 → P3，共 17 项要求。P5 的独立方向字段与结构化假设表单仍暂缓；本批不关闭 #32。

基线为 `main / 059c53a`，适配官方 `dsh-v0.1.5-rc.2 / fb2c4b9e698e30edb738bca4cf0618587db7d203`。插件版本保持 `0.1.5-rc.2.4`，本地开发 Build ID 为 **local-d667a7c2**，不是新的 npm 发布。验收在隔离 profile 安装归档，使用真实 DSH Host、会话持久化和 Chrome；未改动日常 profile。

## 流程与回归证据

| 工作包 | 实现与验收 |
| --- | --- |
| P4.1–P4.5 历史分支 | 工作区／目录／主题原文的完成轮次提供入口，先预览标题和实际继承边界。真实五轮 Agent 讨论从第 2 轮分支，子会话只有两轮；继续输入后只有第三轮新问题，来源仍保持五轮。原生父子关系、主题成员和标题在刷新／正常 Host 重启后保留。切点失效、历史变化、命名／主题失败、独立日志写入中断均恢复同一预留目标。注册视图测试覆盖工作区与主题入口，浏览器验证直接打开子会话和返回原轮次焦点。 |
| P2.1–P2.3 轻量提示 | 找反例、其他方案、改变假设、继续追问均可编辑。已有问题先保留，明确替换或追加才改变内容。四种提示、编辑、取消和恢复有 UI 回归；沿用原有冻结预览、明确确认、同一目标与消息身份。浏览器从同一卡片第 1 版提出两个不同问题，后续研究同时显示实际问题与第 1 版，即使源卡片已更新到第 2 版。 |
| P1.1–P1.6 卡片综合 | 主题内选择 2–3 张明确修订的卡片并冻结完整正文；固定互斥样例保留两方观点、条件与引用。草稿逐条编辑类别、观点和引文；错误形状、错误索引、跨材料和不存在的原句引用被排除并计数，无引用观点保留待验证。手动修改的无效引文就近提示，修正／移除后才能保存。保存新卡片、追加生成、关闭保护、失败重试身份、准确旧修订阅读、搜索／导出／继续讨论均有回归。 |
| P3.1–P3.3 连续原文混选 | 一个连续完整轮次范围计作一项材料，支持两卡＋第 1–2 轮原文，32,000 字符消息预算不静默裁剪。拒绝重复卡片、重叠范围、不可读／未完成／失效边界与数量越界。更新源卡片、移除原文并重启 Host 后仍能审核冻结内容、保存及沿用；导出明确标记原文不可用及保留摘录。 |

综合卡片的有效卡片引用形成“综合自”来源边，图中按目标汇总来源卡片数量，避免交叉连线的标签重叠；连线提示和引用阅读保留准确修订。连续原文继续使用讨论来源边。未引用材料只作为冻结输入保存，不建立虚假关系。全部草稿保存后关闭不再显示多余的未保存提示。

关键测试位于 [历史分支 Host](../../tests/history-branch.harness.spec.ts)、[综合 Host](../../tests/knowledge-synthesis.harness.spec.ts)、[综合／分支界面](../../tests/exploration-views.harness.spec.tsx)、[沿用提示与恢复](../../tests/research-reuse-recovery.harness.spec.tsx)、[注册视图](../../tests/views.client.spec.tsx)、[综合图投影](../../tests/synthesis-graph.client.spec.ts)及[实包新增验收](../../tests/fixtures/exploration-smoke.mjs)。

## 最终检查

本机 Node `26.4.0`、pnpm `11.7.0`：

- `pnpm run check`：24 个文件、198 项通过。
- 匹配官方 RC.2 的 `pnpm check:harness`：源码 Host／Client、发布声明 Host／Client 四个编译面通过；21 个文件、325 项通过。
- `pnpm pack --pack-destination .artifacts` 后运行匹配 `pnpm smoke:harness`：安装、启动、真实持久操作、五轮分支及独立继续、混合综合、导出和移除全部通过；固定模型共 19 次调用。
- `git diff --check` 及三个变更工作台脚本的语法检查通过。

验收归档为 486,973 bytes，SHA-256 `e6e6ae9862ece4fa23579d6a1ff89fd4ff1c33880f269c0831efae5860159d62`。归档、日志、认证入口和临时 profile 保留在私有产物目录，不提交到 Git。

## Chrome 操作与截图

使用本机真实 Chrome 的独立自动化 profile，经真实插件容器操作。覆盖英文／中文、浅色／深色、1440／2048 px 桌面和 800 px 窄窗口；窄窗口内表单、保存栏、选择器及键盘焦点可见。最新构建运行期间捕获的页面错误为 0。

1. 五轮原文选择第 2 轮，核对紧凑标题／继承预览；确认后打开两轮子会话，返回主题显示真实父子边。主题刷新重建原文后，焦点仍返回原第 2 轮入口。
2. 两卡冻结、生成、手改分歧和引文；不存在的原句使保存暂不可用，修正后保存。尝试关闭未保存草稿会保护修改，全部保存后直接关闭。
3. 增加同一讨论第 1–2 轮作为第三项，核对完整冻结范围并另存混合综合。源 A 更新到第 2 版后，从综合引用打开的仍是第 1 版及旧正文。
4. 从源 A 第 1 版切换四种提示，验证已有输入保护；取消后重开保留问题。分别确认“改变权限假设”和“低负载反例”两个实际问题，卡片后续研究和主题图出现两个独立沿用目标。

| 截图 | 内容 |
| --- | --- |
| [历史分支预览](assets/issue32/history-branch-preview.png) | 中文浅色，原第 2 轮与紧凑确认界面 |
| [桌面综合编辑](assets/issue32/synthesis-edit-desktop.png) | 英文浅色，人工修改观点和引用 |
| [混合范围预览](assets/issue32/synthesis-mixed-preview.png) | 两张卡片和连续第 1–2 轮原文 |
| [冻结旧修订](assets/issue32/synthesis-frozen-revision-dark.png) | 中文深色，来源已有第 2 版但引用仍打开第 1 版 |
| [主题关系图](assets/issue32/synthesis-graph-desktop.png) | 历史父子、卡片综合与双问题沿用关系并存 |
| [窄屏提示](assets/issue32/prompts-narrow-dark.png) | 中文深色，四种提示、可编辑问题和可见键盘焦点 |
| [窄屏后续研究](assets/issue32/two-followups-narrow-dark.png) | 同一卡片的两个实际问题与固定修订 |
| [窄屏综合编辑](assets/issue32/synthesis-narrow-light.png) | 中文浅色，观点／来源／原句编辑和保存栏 |

固定示例只验证产品行为。另通过 `deepseek-official / deepseek-v4-flash` 对两张合成互斥观点卡片进行一次真实模型审核：8 条观点、12 处有效引用，人工收窄 2 处分类／表述，未自动保存生成结果。输入、输出、遗漏核对和修订详见[真实模型审核](issue32-model-quality.md)。该单例不代表稳定准确率，也不作为 CI 硬门槛。

领域决定见 [ADR 0014](../adr/0014-recover-historical-branches-by-reserved-identity.md) 和 [ADR 0015](../adr/0015-synthesize-frozen-materials-with-quoted-claims.md)。跨浏览器重启的未保存草稿恢复、跨 Host 汇聚和 P5 的结构化方向字段不在本批范围。
