# PR #17：原文位置恢复修复验收

日期：2026-09-10。修复范围 `961b05d…25861bf`，最终功能提交 `25861bfd55721c027679a72b0084e9094971f521`。主题中的知识来源节点现在恢复上次有效的原文阅读窗口和滚动位置；卡片内显式点击来源仍打开该修订保存的精确范围。本记录补充[第二轮修复验收](pr-17-followup.md)。

## 修复与复审

| 问题 | 结果 |
| --- | --- |
| P2：主题来源的保留摘录覆盖上次阅读位置 | 区分显式来源地址与主题节点的保留来源。恢复优先使用已保存的阅读位置；连接失败时保留摘录，重试核验原文成功后才恢复滚动。 |
| 同一 P2 的长来源边界：重开只加载默认 10 轮 | 显式来源和复核窗口记住起止事件边界，Host 重新核验并读取完整范围。浏览器只保存数字边界。普通分页清除范围并保留原有刷新行为；失效范围清空阅读位置和选择。 |
| P3 启发式：知识及沿用 codec 重复基础校验 | 提取无 Host 依赖的共同校验，保留各领域的错误消息、长度限制和 schema。 |

独立 Standards／Spec reviewer 对最终完整修复范围复核：原 P3、原 P2 及其长来源余项均关闭，新增问题 0，两轴均无剩余严重级别。首次修复后的独立复核发现 12 轮来源恢复成 10 轮，补修后再次通过；结论以最终范围为准。

## 自动验证

| 检查 | 结果 |
| --- | --- |
| 新增回归 | 8 项先复现再修复：普通恢复、连接失败重试、失效来源回退、12 轮来源、复核第 2–12 轮、卡片来源修订变短、真实来源移除，以及精确范围 Gateway 验证。 |
| `pnpm run check` | 类型、构建、20 文件／175 项独立测试通过。 |
| 匹配 `check:harness` | Host／Client 源码及公开声明四个编译面、13 文件／246 项通过。 |
| codec 行为对照 | 与 `961b05d` 比较 19 个公开 schema、7,428 组输入，返回值、错误类型和消息全部一致。 |
| 新归档 packed smoke | 安装、启动、真实 Gateway 范围恢复、持久知识及原生沿用、卸载全部通过；固定模型调用 10 次。 |

长范围注册 Graph 回归通过真实 Session、History Service 和 Gateway 读取，避免在界面模拟中掩盖默认分页上限。Gateway 回归另验证无效边界、互斥定位参数、额外字段拒绝，以及来源事件和模型调用不变。原来的单轮复现与长范围复现均保留了失败记录。

## 安装包与浏览器

| 项目 | 值 |
| --- | --- |
| 插件／Harness | `0.1.5-alpha.1`／`dsh-v0.1.5-alpha.1`，Harness `5dda764ed3aa172535a7967b06ff95d9cbfe536a` |
| 受测 Build | `local-a8647d58` |
| 浏览器／视口 | Chrome `153.0.8010.36`，1440×1080 |
| 归档 | `benz-ai-x-dsh-client-ui-session-graph-0.1.5-alpha.1.tgz`，357,740 字节 |
| SHA-256 | `471248ab0a3ddaed4924f6bdf93b9cc62713c09f935fa0f18598550269654623` |

实际安装模块的 `lib/client.js` 与本地最终构建逐字节一致。使用独立临时 profile，通过真实 Host 准备三个各 12 轮的固定讨论，以及单轮来源和 12 轮来源两张卡片。

1. 在主题来源节点打开原文，从保存的第 1 轮翻到后续页面。真实鼠标滚轮滚动至 550 像素，关闭 Graph 后明确重开主题，Host 请求恢复游标 `20`，滚动仍为 550 像素。
2. 在同一路径注入一次 Gateway 连接失败。界面显示读取失败及“仅存摘录”，已保存滚动位置仍在。重试后恢复相同原文页面和 550 像素位置。
3. 在卡片内点击来源，仍请求该修订的事件 `5–17`，不受主题的翻页游标影响。
4. 在长来源主题中阅读全部 12 轮，并滚到第 12 轮附近。关闭 Graph 后重开，Host 按范围 `5–106` 读取，全部 12 轮仍在；前后滚动位置均为 14,726 像素，内容高度均为 16,287 像素。
5. 令该来源的原文读取暂时不可用，重开后清除选择及范围、游标、滚动记录，保留视口并提示。明确重新选择来源后仍可阅读保留摘录。随后恢复原文读取。

上述浏览器流程没有新增模型调用；准备讨论及标题共 39 次固定模型调用。三个来源各 108 条事件的快照哈希前后一致，页面错误为 0。未使用真实模型凭据；本记录验证功能，不作模型质量结论。

## 截图与原始记录

五张截图均来自最终 Build，已逐张检查：

| 截图 | 内容 |
| --- | --- |
| [普通位置恢复](../assets/pr-17-position/original-restored.png) | 后续原文页面恢复到 550 像素。 |
| [连接失败时保留摘录](../assets/pr-17-position/retained-during-retry.png) | 错误、摘录标识及重试入口。 |
| [长来源关闭前](../assets/pr-17-position/long-source-before.png) | 第 12 轮附近的阅读位置。 |
| [长来源重开后](../assets/pr-17-position/long-source-restored.png) | 全部范围及相同的深处滚动位置。 |
| [原文不可用后的摘录](../assets/pr-17-position/missing-source-excerpt.png) | 失效选择提示与明确重选后的保留摘录。 |

忽略目录 `.artifacts/pr17-position-fix/` 保存 `position-red.log`、`range-red.log`、`range-green.log`、`check.log`、`check-harness.log`、`codec-differential.log`、`smoke.log` 和 `browser-evidence.json`。`*-before-range.log` 及原长来源探针记录的是补修前状态。浏览器脚本曾因提示内包含关闭按钮文字而精确匹配超时；修正定位方式后在同一 profile 完成余下步骤，产品代码未因此改变。

自有浏览器和临时 Host 已退出，临时 profile 及认证状态文件已删除。本轮未合并 PR、发布版本或修改用户实际 profile。
